describe("Setting up the KNOCKOUT project", function()
{
  it("should define a global constant", function()
  {
    expect(KNOCKOUT).toBeDefined();
  })

  it("should define a short-hand alias", function()
  {
    expect(NKO).toBeDefined();
  })
})